# GAD AI Terminal — Память проекта
> Быстрая навигация по проекту. Обновлено: **23.06.2026 ночь**.
> Читай это в начале каждой сессии чтобы понять что к чему.

---

## CHANGELOG 23.06.2026 ночь — Polymarket починен, патч-система, диагностика ботов

### Polymarket-bot: починен модуль openai
- **Проблема:** `Cannot find module 'openai'` — image был собран ДО того как openai добавили в package.json
- **Фикс:** `docker stop → docker run --entrypoint sleep → npm install openai → docker commit → docker compose up`
- Бот запущен: `gad-ai-polymarket-bot Up` ✅
- Gemini Flash добавлен как шаг 0 в LLM цепочке (`GEMINI_API_KEY` в VPS .env)
- Polymarket остаётся в DRY-RUN до WR≥65% на 30 сделках

### Патч-система (docker-compose.override.yml + patches/)
- **Решает проблему:** при `docker compose up -d --no-deps service` container пересоздаётся из IMAGE → hot-patched dist теряется
- `docker-compose.override.yml` — монтирует `./patches/` read-only + переопределяет CMD для autobuy и base-scanner
- `patches/autobuy-start.sh` + `patches/base-scanner-start.sh` — запускают patch.js перед стартом сервиса
- Патчи идемпотентны — безопасно применяются при каждом рестарте

### Патчи autobuy (patches/autobuy.patch.js):
1. **bonding-smart shadow INSERT** — записывает WOULD-BUY в shadow_trades
2. **PumpPortal reconnect 5s → 60s** — bonding-smart (снижает шанс IP-бана)
3. **auto-signal FEAR floor 35k → 25k** — $27-33k зона прибыльна (VPS имел старый компилят с $35k)
4. **auto-signal max liq 80k → 120k** — токены вроде WOJAK $107k теперь не блокируются
5. **grad-scan reconnect 15s → 60s** — меньше запросов к заблокированному PumpPortal

### Диагностика нулевых сигналов (НЕ БАГ — рыночные условия)
| Бот | Реальная причина нуля |
|---|---|
| **Raydium** | F&G=23, боковик. DexScreener возвращает только старые токены. Все `✗age` или `✗liq (vol=0)`. `mom=0` = ни один не дошёл до проверки momentum |
| **bonding-smart** | PumpPortal WebSocket **403 — IP-бан Hetzner**. Reconnect 60s поможет разбанить. Нет данных → нет сигналов |
| **grad-scan** | Тоже 403, reconnect был 15s (много запросов) → теперь 60s |

**Раздражающий факт:** PumpPortal банит VPS IP при частых реконнектах (было 5s/15s). После перехода на 60s IP должен разблокироваться за 15-30 мин.

### VPS .env (добавлено в этой сессии):
```bash
BASE_AUTO_BUY=false          # Base scanner — DRY-RUN (тень-режим)
BASE_POLL_INTERVAL_MS=3000   # мониторинг позиций каждые 3s (было 10s)
GEMINI_API_KEY=<redacted — set in VPS .env>  # Gemini Flash LLM
```

### Текущее состояние ботов (23.06.2026 ночь)
| Бот | Статус | Детали |
|---|---|---|
| **Raydium (MASTER W1)** | ✅ Сканирует | F&G=23, mom=0, ждёт momentum. Фильтры исправлены |
| **bonding-smart (LAUNCH-2 W2)** | ⚠️ WS 403 | PumpPortal IP-бан. Reconnect 60s. Восстановится само |
| **Base scanner** | ✅ DRY-RUN | BASE_AUTO_BUY=false, shadow mode активен, 3s monitor |
| **Polymarket** | ✅ DRY-RUN | Починен, Gemini активен. Ждём 30 сделок для LIVE |
| **TON scanner** | ✅ DRY-RUN | TON_AUTO_BUY=false |
| **BSC scanner** | ⚠️ Кошелёк пуст | BSC_AUTO_BUY=false |
| **Futures** | ✅ Paper | Paper mode |

---

## CHANGELOG 23.06.2026 вечер — Фильтры смягчены, shadow добавлен

### bonding-smart v2: фильтры смягчены
- **Velocity:** 6 SOL/60s → **3 SOL/60s** (было слишком жёстко для текущего рынка)
- **Min unique buyers:** 40 → **20** (токены с Twitter проходили social, но отклонялись по buyers)
- **Shadow mode добавлен:** INSERT в `shadow_trades` перед каждым реальным buy (chain=solana, strategy=bonding_smart)
- Социальный фильтр ОСТАВЛЕН: Twitter ИЛИ Telegram обязательно
- Конфиг: `Config: 0.005 SOL/trade, curve 25-55%, vel ≥3 SOL/60s, buyers ≥20`

### Raydium: расширены фильтры для получения сигналов
- **Min liq:** 25k (baked) → **12k** (хардкод в JS — VPS .env says 12k но container baked 25k)
- **Max liq:** 80k → **120k** (GTAVI имел $106k + pc1h=5.2% — теперь проходит)
- **Max age:** 6h (baked) → **12h** (RUST age 10.8h теперь проходит liq+age, отклоняется только если нет momentum)
- Результат: RUST теперь `✗mom pc5m=-1.0%` вместо `✗age` — правильно (откат не покупаем)

### Base scanner: MAX_PC1H=30 (стоп для уже-пампанувших)
- Было: default 100% (нет ограничения сверху)
- Стало: **30%** — не покупаем токены с pc1h >30%
- Причина: B20 куплен при pc1h=55% → упал -47% через 1ч (входили в распределение)
- Fresh токены (<1ч) НЕ затронуты: `MAX_PC1H_FRESH=1000` для них

### Диагноз Raydium 4-day silence (не баг — условия рынка)
- F&G=23, боковик. Из 80 токенов/цикл: 54 `✗liq`, 19 `✗age`, `mom=0`
- `mom=0` означает: 7 токенов проходят liq+age НО pc1h <5% — рынок просто не растёт
- С новыми фильтрами (12k/120k/12h): больше токенов в пуле, RUST теперь ✗mom вместо ✗age
- Когда F&G > 35 и появятся токены с momentum → бот начнёт торговать

### VPS patches (hot-patch, без rebuild)
Файлы в `/usr/src/app/services/autobuy/dist/`:
- `bonding-smart.js:116` → `|| '3'` (velocity)
- `bonding-smart.js:118` → `|| '20'` (buyers)
- `bonding-smart.js:546` → shadow INSERT before doBuy()
- `auto-signal.js:378` → `RAYDIUM_MIN_LIQUIDITY_USD = 12000`
- `auto-signal.js:382` → `RAYDIUM_MAX_LIQUIDITY_USD = 120000`
- `auto-signal.js:371` → `maxAge = 43200` (checkFreshToken inner)
- `auto-signal.js:392` → `RAYDIUM_MAX_AGE_SEC = 43200`
- `base-scanner/dist/scanner.js:39` → `|| '30'` (MAX_PC1H)

---

## CHANGELOG 23.06.2026 — Что изменилось сегодня

### Номенклатура кошельков (ФИНАЛЬНАЯ — больше не менять)

| Имя | Кошелёк | ENV var | Роль |
|---|---|---|---|
| **MASTER** (W1) | EL4mS7Xg... | `WALLET_PRIVATE_KEY` | Raydium autobuy — единственный прибыльный |
| **LAUNCH-2** (W2) | CFmHWpmQ... | `PUMPFUN_WALLET_PRIVATE_KEY` | bonding-smart pump.fun трейдинг (новое!) + launchTriple |
| **LAUNCH-3** (W3) | DJ8Tq8vi... | `PUMPFUN_WALLET_PRIVATE_KEY_2` | ТОЛЬКО /auto_launch — **hardcoded запрет на трейдинг** |

**Реальные балансы (23.06.2026):**
- MASTER: **0.1789 SOL** (данные в CLAUDE.md были устаревшими от 14.06)
- LAUNCH-2: **0.0403 SOL**
- LAUNCH-3: **0.0038 SOL** (почти пусто — только для launches)

### bonding-smart v2 АКТИВИРОВАН (pump.fun через LAUNCH-2)

**Что сделано:**
- `BONDING_SMART_ENABLED=true` + `BONDING_SMART_USE_W2=true` → LAUNCH-2 (CFmHWpmQ) торгует
- `RESIDENTIAL_PROXY_URL=http://qcbmhugq:yheaovnrz813@31.59.20.176:6754` — обходит Hetzner IP-ban PumpPortal
- Proxy работает: `[bonding-smart] Proxy: HTTPS http:***@31.59.20.176:6754`
- WebSocket подключён: `[bonding-smart] WebSocket connected`
- `BONDING_SMART_BUY_SOL=0.005`, `BONDING_SMART_MAX_POSITIONS=1`

**Архитектура bonding-smart v2 (файлы в autobuy/src/):**
- `bonding-smart.ts` — главный движок (WebSocket PumpPortal, buy/sell)
- `dev-profiler.ts` — serial-scammer detection по onchain истории
- `sybil-detector.ts` — bundle + co-funded wallet detection
- `smart-money-tracker.ts` — copy-trade прибыльных кошельков
- `jito-helper.ts` — Jito MEV bundle anti-frontrun
- `social-sentiment.ts` — Twitter/TG presence check
- `websocket-guard.ts` — auto-reconnect WebSocket с proxy

**Фильтры:** curve 25-55%, vel ≥3 SOL/60s (23.06), buyers ≥20 (23.06), dev ≤3%, stop 8%, jito:true

**Исправлен баг:** `getKeypair()` теперь корректно использует `PUMPFUN_WALLET_PRIVATE_KEY` (W2) при `BONDING_SMART_USE_W2=true` (был `_2` → W3 — неправильно!)

### HOT poller ОТКЛЮЧЁН (защита LAUNCH-3)

- `BONDING_HOT_ENABLED=false` в .env
- В compiled `bonding-scanner.js` захардкожено `false` (container не читал новый .env)
- **LAUNCH-3 (DJ8Tq8vi) больше не участвует в трейдинге** — только /auto_launch

### Raydium: FEAR режим смягчён

- `RAYDIUM_MIN_PC1H=5` (было 10)
- Убран hardcoded override в FEAR: было `Math.max(RAYDIUM_MIN_PC1H, 10)` → теперь `RAYDIUM_MIN_PC1H`
- Raydium начнёт покупать при F&G=20 как только появятся токены с pc1h ≥5%

### Shadow Mode РАЗВЁРНУТ (из прошлой сессии)

- `migration 027_shadow_trades.sql` применён на VPS — таблица `shadow_trades`
- Все 4 бота записывают WOULD-BUY события (без реальных сделок)
- `/shadow_report` в Telegram боте — статистика P&L по стратегиям
- Raydium: записывает при `RAYDIUM_DRY_RUN=true`
- Base: записывает при `BASE_AUTO_BUY=false`
- BSC: записывает при `BSC_AUTO_BUY=false`
- bonding-smart: отдельная shadow логика

### VPS .env изменения 23.06.2026
```bash
BONDING_SMART_ENABLED=true        # ранее false
BONDING_SMART_USE_W2=true         # ранее false — LAUNCH-2 торгует
BONDING_SMART_BUY_SOL=0.005       # 0.005 SOL/trade (безопасно для 0.04 SOL баланса W2)
BONDING_HOT_ENABLED=false         # ранее true — HOT poller отключён
RAYDIUM_MIN_PC1H=5                # ранее 10 — FEAR режим смягчён
RESIDENTIAL_PROXY_URL=http://qcbmhugq:yheaovnrz813@31.59.20.176:6754  # уже был
```

### Текущее состояние ботов (23.06.2026 утро)

| Бот | Статус | Детали |
|---|---|---|
| **Raydium (MASTER)** | ✅ Сканирует | F&G=20, mom=0 (рынок боковой), RAYDIUM_MIN_PC1H=5 теперь |
| **bonding-smart (LAUNCH-2)** | ✅ v2 ACTIVE | Proxy работает, WS подключён, ждёт сигналов (❌nosocial пока) |
| **Base scanner** | ✅ DRY-RUN | cbXRP очищен, BASE_AUTO_BUY=false |
| **BSC scanner** | ⚠️ Кошелёк ~пуст | BSC_AUTO_BUY=false |
| **Polymarket** | ✅ DRY-RUN | Groq→DeepSeek→OpenAI fallback |
| **TON scanner** | ✅ DRY-RUN | TON_AUTO_BUY=false |
| **Futures** | ✅ Paper | ADX фильтр активен |
| **LAUNCH-3** | ✅ Launch-only | HOT отключён, только /auto_launch |

---

## CHANGELOG 22.06.2026 — Что изменилось сегодня

### Security Shield v2 — Все 4 сети (коммит 2cf6348b)

| Модуль | Файл | Что блокирует |
|---|---|---|
| **Solana Gate 7** | `libs/solana/src/safety-guard.ts` | MintAuthority≠null, FreezeAuthority≠null, LP Burn <90% |
| **EVM Security Shield** | `libs/evm/src/security-shield.ts` | GoPlus: honeypot, mintable, tax>12%, unverified contract. Перенесён из service-local в shared lib |
| **EVM Swap Simulator** | `libs/evm/src/swap-simulator.ts` | Виртуальный buy→sell через V2 getAmountsOut — ловит tax-honeypots которые GoPlus пропускает |
| **TON Validator** | `libs/ton/src/safety-validator.ts` | On-chain get_jetton_data: mintable=false + admin=null |
| **Polymarket Validator** | `services/polymarket-bot/src/market-validator.ts` | Vol≥$50k, spread 0.88-1.12, max 14 дней |

**Интеграции:**
- `services/autobuy/src/auto-signal.ts` — Gate 7 добавлен после Gate 6 (top holders)
- `services/base-scanner/src/index.ts` — SwapSim блокирует перед buyToken()
- `services/polymarket-bot/src/index.ts` — validatePolymarketMarket() в tryOpenPosition()
- `libs/evm/` — новый npm workspace (package.json + tsconfig.json)
- `services/base-scanner/src/security-shield.ts` — УДАЛЁН (код перенесён в libs/evm)

### Polymarket scorer: Groq API подключён (hot-patch)
- `GROQ_API_KEY=<redacted — set in VPS .env>` добавлен в VPS .env
- `scorer.ts` обновлён в контейнере: `callLLMFree()` цепочка Groq → DeepSeek → OpenAI
- Подтверждено: `[poly-scorer] Groq (429) → DeepSeek (402) → OpenAI` — Groq используется первым
- Groq free tier: 6000 TPM, 5.5s serializer предотвращает burst 429

### Security Shield v2.1 — TON holder guard + BSC 5% tax (коммит 966cef3e)

| Изменение | Файл | Детали |
|---|---|---|
| **TON Holder Concentration Guard** | `libs/ton/src/discovery.ts` | `checkTonHolderConcentration()` — TonAPI v2, топ-5 холдеров ≤45%, 10-мин кеш, fail-open |
| **TON scanner интеграция** | `services/ton-scanner/src/scanner.ts` | Проверяется ПОСЛЕ filterToken(), ДО safety check |
| **TON min_liq** | VPS .env | `TON_MIN_LIQUIDITY_USD=35000` (было 3000) — тонкие пулы = проскальзывание |
| **BSC tax limit 5%** | `libs/evm/src/security-shield.ts` | chainId=56 → maxTax=5%, chainId=8453 → 12%. Математика: 12+12% round-trip = нужен +30% для безубыточности |
| **cbXRP очищен** | SQL | `UPDATE base_positions SET is_active=false WHERE symbol='cbXRP'` — слот освобождён |

**VPS .env (22.06):** `TON_MIN_LIQUIDITY_USD=35000`

### Симуляция 50 сделок × 2000 итераций (scripts/simulate-all-bots.ts)

| Бот | Медиана P&L | Sharpe | % прибыльных | Вывод |
|---|---|---|---|---|
| **Raydium** | +0.0320 SOL | 1.55 | 92.8% | ✅ Единственный реально прибыльный |
| **Base EVM** | +0.0006 ETH | 0.81 | 81.3% | ✅ DRY-RUN — нужны реальные данные |
| **BSC EVM** | +0.0001 ETH | 0.16 | 56.9% | ⚠️ Пограничный |
| **TON** | -0.465 TON | -1.28 | 8.2% | ❌ WR 28% — не включать |
| **Polymarket** | +$23.5 USDC | 0.86 | 82.9% | ✅ DRY-RUN (1/30 сделок пока) |

### Реальный статус ботов на 22.06.2026 (из логов VPS)

| Бот | Реальный статус | Причина |
|---|---|---|
| **Raydium autobuy** | ✅ Работает, покупок НЕТ | F&G=20 (FEAR), все токены ✗mom=0 (рынок боковой) |
| **bonding-smart** | ✅ Видит токены, НЕ покупает | Все ❌nosocial — нет Twitter-сигнала |
| **Base scanner** | ✅ DRY-RUN, 1 позиция | cbXRP @ 0.988x (под водой). BASE_AUTO_BUY=false |
| **BSC scanner** | ✅ Сканирует, НЕ покупает | Все ✗mom (нет объёма) |
| **Polymarket** | ✅ Groq fallback работает | DRY-RUN, 1/30 сделок |
| **TON scanner** | ✅ DRY-RUN | Все seen: (seen-cache). TON_AUTO_BUY=false |
| **Futures** | ✅ Paper, 1 позиция открыта | Не открывает новые пока есть открытая |

**Почему Raydium не покупает:** F&G=20, Market Health=28/100. Scan-цикл: liq=72-75 токенов прошли liq gate, но mom=0 — нет ни одного токена с достаточным momentum (pc1h ≥5%). Рынок флэт/боковой — нет сигналов.

### VPS .env изменения 22.06.2026
```bash
GROQ_API_KEY=<redacted — set in VPS .env>  # ✅ добавлен
```

---

## CHANGELOG 20.06.2026 — Что изменилось сегодня

### Новые функции
| Компонент | Изменение |
|---|---|
| `services/social-monitor/src/free-aggregator.ts` | **НОВЫЙ** — Reddit JSON API + DexScreener Profiles + Telegram web mirrors, local keyword classifier, Claude только для AI_AGENT+POLITICS (экономия 98% API кредитов) |
| `services/social-monitor/src/monitor.ts` | `runFreeAlphaCycle()` каждые 5 минут |
| `services/polymarket-bot/src/scorer.ts` | `callLLM()` — Claude → OpenAI gpt-4o-mini fallback при нехватке кредитов |
| `services/polymarket-bot/src/rss-sync.ts` | **НОВЫЙ** — 5 бесплатных RSS лент (CoinDesk, CoinTelegraph, Decrypt, Reuters, BBC) |
| `services/polymarket-bot/src/index.ts` | Strategy 3 = RSS news signals каждые 10мин |
| `services/futures/src/entry-strategy.ts` | ADX(14) с Wilder smoothing — блокирует LONG/SHORT при слабом тренде (<22) |
| `services/futures/src/types.ts` | adx, plusDI, minusDI в TechnicalSignal |
| `migrations/023_raw_alpha_feeds.sql` | **НОВЫЙ** — таблица для Free Alpha Aggregator, auto-cleanup 7 дней |

### VPS .env (добавлено 20.06)
```bash
OPENAI_API_KEY=sk-proj-...   # ✅ добавлен — fallback для Polymarket scorer + trend-launcher
TON_MIN_LIQUIDITY_USD=3000   # ✅ добавлен — фильтр TON токенов
TON_MAX_LIQUIDITY_USD=100000 # ✅ добавлен
POLYMARKET_WALLET_PRIVATE_KEY=0xd3b2... # ✅ добавлен (Polygon кошелёк для LIVE режима)
```

### Оплата (обновлено 20.06.2026 — полный переход с SOL)
| Метод | Детали |
|---|---|
| USDT на BSC | Treasury: `0x4C0B07Ad19D47994639D18ac2Af2FF82A0F95F37`, BSC USDT 18 decimals |
| Telegram Stars | trial_1d=219⭐ \| trial_3d=437⭐ \| monthly=4367⭐ (rate $2.29/100⭐) |
| SOL (legacy) | оставлен для обратной совместимости |

### Тарифы (актуально 20.06.2026)
| slug | Цена USD | Stars | Срок |
|---|---|---|---|
| `trial_1d` | $5 | 219⭐ | 24 часа |
| `trial_3d` | $10 | 437⭐ | 72 часа |
| `monthly` | $100 | 4367⭐ | 30 дней |

### Состояние систем на 22.06 утро
| Система | Статус |
|---|---|
| **Raydium autobuy** | ✅ АКТИВЕН — покупок нет (F&G=20, mom=0, боковой рынок) |
| Polymarket | ✅ DRY-RUN, 94 рынка, **Groq + DeepSeek + OpenAI** fallback (Groq первый!) |
| Solana Gate 7 | ✅ НОВОЕ — MintAuth + FreezeAuth + LP Burn ≥90% в auto-signal.ts |
| EVM Security Shield | ✅ НОВОЕ — GoPlus + Swap Simulator в base-scanner (libs/evm) |
| TON Jetton Validator | ✅ НОВОЕ — mintable + admin check (пока не интегрирован в ton-scanner scan loop) |
| X Trend pipeline | ✅ Нитер RSS 3 аккаунта (без engagement метрик), GDELT fallback |
| Free Alpha Aggregator | ✅ Reddit+DexScreener+Telegram, работает каждые 5мин |
| ADX Futures filter | ✅ adx>22 + DI alignment gate = меньше ложных сигналов |
| BSC scanner | ⚠️ Кошелёк пустой — торговля невозможна |
| TON scanner | ⏳ DRY-RUN, фильтры $3k-$100k |
| Base scanner | ⏳ DRY-RUN, cbXRP @ 0.988x открыт |
| Disk VPS | ✅ 16% заполнен (32GB свободно) — проблема решена |

---

## CHANGELOG 19.06.2026 — Что изменилось вчера

### Код (задеплоено hot-patch)
| Файл | Изменение |
|---|---|
| `auto-signal.ts` | MIN_LIQ 22k→30k, age gate расширен до liq<$45k, FEAR floor 25k→35k, vol velocity Gate 3d |
| `x-trends.ts` | Числа убраны из word extraction, STOP_WORDS расширен, GENERAL темы пропускаются |
| `scheduler.ts` | `cleanupPhantomPositions()` при старте — нет фантомов в active pool |
| `bsc-scanner/src/index.ts` | Gas reserve фикс: `BUY_BNB+0.005`, дублирующий buy check |

### VPS .env (обновлено вчера)
```bash
RAYDIUM_MIN_LIQUIDITY_USD=30000   # было 22000
RAYDIUM_MIN_VOL5M_LIQ=0.15       # новый vol velocity filter
ANTHROPIC_API_KEY=<SET>           # ✅ теперь работает trend→AI idea pipeline
```

---

---

## Что это за проект

**GAD AI Terminal** — Solana/Base/BSC/TON мемкоин платформа:
- **Автоматическая торговля:** находит токены, покупает, продаёт по TP/SL
- **Аналитика:** скоры, скринеры, whale-трекер, социальные сигналы
- **Запуск монет:** авто-лончер по трендам X, ручной запуск
- **Telegram-бот:** @gadai_sol_bot — весь доступ для пользователей
- **Лендинг:** gadai.shop (Vercel) — подписки, оплата SOL

**Деплой:** VPS Hetzner `root@65.21.159.255`, ключ `~/.ssh/gad_deploy`
**Git:** `git push gad main` → github.com/PonchoGAD/GAD-AI-Terminal → VPS тянет оттуда
**Лендинг:** `git push gadai main` → github.com/PonchoGAD/gadai.git → Vercel

---

## Сервисы (docker compose, все на VPS 24/7)

| Сервис | Порт | Что делает |
|---|---|---|
| `api` | 4000 | REST API: токены, подписки, TG-linking, /unified-portfolio |
| `scanner` | — | DexScreener + GeckoTerminal + Birdeye → PostgreSQL |
| `autobuy` | — | Raydium scanner + Bonding curve (SOL Velocity) + Sell loop |
| `telegram` | — | Бот @gadai_sol_bot — все команды + Alpha Engine |
| `whale-tracker` | — | Helius webhook мониторинг крупных кошельков |
| `social-monitor` | — | KOL Twitter + X тренды + **авто-лончер токенов** |
| `dashboard` | 3000 | Next.js фронтенд (внутренний) |
| `landing` | 3001 | gadai.shop — мультилокаль en/ru, pricing, pay |
| `futures` | 4003 | BTC macro + Drift Protocol (paper mode) |
| `base-scanner` | 4005 | Base Network EVM (dry-run) |
| `ton-scanner` | 4007 | TON Network STON.fi (dry-run) |
| `bsc-scanner` | — | BSC/PancakeSwap (dry-run, отключён) |

---

## Торговые стратегии (что реально покупает)

### Активные стратегии (23.06.2026)

| Стратегия | Сервис | Вкл | Параметры |
|---|---|---|---|
| **Raydium Fresh scanner** | autobuy/MASTER | ✅ | liq $30-80k, age 1-6h, pc1h ≥5%, vol5m/liq ≥15% |
| **bonding-smart v2** | autobuy/LAUNCH-2 | ✅ | curve 25-55%, vel ≥6 SOL/60s, buyers ≥40, dev ≤3%, stop 8% |

### Отключённые НАВСЕГДА

| Стратегия | P&L | Причина |
|---|---|---|
| SOL Velocity Tracker | -0.455 SOL (26 trades, 3.8% WR) | DexScreener lag, false positive whale spikes |
| HOT MOVER poller | -0.447 SOL | DexScreener lag 30-60s = покупка на пике |
| GRAD Hunter | -0.112 SOL | Покупка перед листинг-дампом |
| Score-80 Signal | -100% на всех | Jupiter не продаёт pump.fun токены |
| Whale Signal | -54% | Нерелевантные сигналы |
| T2 Raydium (liq>80k) | 0% WR (5 trades) | 0% win rate из аудита |

### Параметры выхода (Raydium) — актуальные

```
T1 (liq $30-80k) FEAR:  TP1 1.22x (60%), TP2 1.48x (50%), Stop 8%, Trail 12%
T1 (liq $30-80k) BULL:  TP1 1.15x (20%), TP2 1.35x (25%), TP3 1.55x (25%)
T1 NEUTRAL:             TP1 1.12x (20%), TP2 1.28x (25%)
Early trail: 4% если позиция > 1.10x
Breakeven: после TP1 → стоп на entry_price + комиссии
```

---

## Ключевые файлы

### autobuy сервис

```
services/autobuy/src/
  auto-signal.ts        ← Raydium scanner (MASTER кошелёк, processAutoSignals ОТКЛЮЧЁН)
  scheduler.ts          ← Sell loop (1s интервал) + shadow checker
  bonding-smart.ts      ← pump.fun v2 (LAUNCH-2 кошелёк, WebSocket + Proxy)
  dev-profiler.ts       ← serial-scammer detection
  sybil-detector.ts     ← bundle + co-funded wallet detection
  smart-money-tracker.ts← copy-trade прибыльных кошельков
  jito-helper.ts        ← Jito MEV bundle anti-frontrun
  websocket-guard.ts    ← auto-reconnect WebSocket с proxy
  bonding-scanner.ts    ← Position sell loop (HOT поллер ОТКЛЮЧЁН)
  graduation-scanner.ts ← WS PumpPortal (GRAD выключен, нужен pre-grad exit)
```

### social-monitor сервис

```
services/social-monitor/src/
  monitor.ts            ← Main loop: KOL poll + X trends + авто-лончер
  trend-launcher.ts     ← X Trend → Claude → Pollinations → Pinata → pump.fun
  x-trends.ts           ← Twitter API + GDELT fallback (final_score ≥ 30)
  coin-hunter.ts        ← DexScreener поиск монеты под тему
```

### Библиотеки (libs/)

```
libs/
  db/          → pg pool, query(), transaction()
  solana/      → RPC, Helius, token metadata
  autobuy/     → Jupiter swap: loadKeypair, executeSwap
  scoring/     → AI-скор (6 факторов)
  risk/        → Риск-скор
  rug/         → Rug-pull флаги (9 метрик)
  gad-score/   → Единый рейтинг 0-100 (LEGENDARY/STRONG/GOOD...)
  narrative/   → Определение нарратива (AI_AGENT/DOG/PEPE...)
  social/      → Hype-скор
  survival/    → Вероятность выживания
  dna/         → Классификация кошелька (SNIPER/WHALE/INSIDER...)
  alerts/      → Alert engine
  lifecycle/   → Стадии токена (BIRTH→DEATH)
  opportunity/ → Pre-breakout alpha
  memory/      → Сравнение с историческими 100x
  regime/      → Рыночный режим (BULL/BEAR/SIDEWAYS/EUPHORIA/PANIC)
  reputation/  → Классификация кошельков
  trend-engine/→ GDELT + Google News → кластеры → AI идеи
```

---

## База данных (23 таблицы)

```sql
-- Торговля
tokens, token_metrics, autobuy_jobs, autosell_stages
score_history, alerts

-- Сети
base_positions, bsc_positions, ton_positions

-- Подписки
subscriptions, subscription_plans, telegram_users

-- Тренды и лончер
x_trend_signals, trend_items, trend_clusters, coin_ideas
coin_launches (если migration применена)

-- Другое
whale_scores, monitored_accounts, social_signals, social_metrics
```

---

## Кошельки

| Имя | Адрес | ENV | Роль | Баланс (23.06.26) |
|---|---|---|---|---|
| **MASTER** (W1) | EL4mS7Xg... | WALLET_PRIVATE_KEY | **Raydium autobuy** (единственный прибыльный) | 0.1789 SOL |
| **LAUNCH-2** (W2) | CFmHWpmQ... | PUMPFUN_WALLET_PRIVATE_KEY | **bonding-smart pump.fun** + launchTriple | 0.0403 SOL |
| **LAUNCH-3** (W3) | DJ8Tq8vi... | PUMPFUN_WALLET_PRIVATE_KEY_2 | **ТОЛЬКО /auto_launch** — hardcoded запрет на трейдинг | 0.0038 SOL |

> **ВАЖНО:** Только LAUNCH-3 теперь является "launch-only". LAUNCH-2 торгует через bonding-smart v2 с proxy.

---

## Тарифные планы

| Slug | Цена | Срок |
|---|---|---|
| trial_1d | 0.05 SOL | 24 часа |
| trial_3d | 0.1 SOL | 72 часа |
| monthly | 1.0 SOL | 30 дней |
| autobuy_pro | 5.0 SOL | 30 дней (планируется) |

---

## VPS — критические факты

```bash
IP:          65.21.159.255
SSH:         ssh -i ~/.ssh/gad_deploy root@65.21.159.255
Проект:      /opt/gad-ai-terminal/GAD-AI-Terminal
Диск:        38GB, 16% заполнен (32GB свободно — OK 20.06.2026)
RAM:         увеличена в Hetzner
PostgreSQL:  порт 5432

Профилактика диска (раз в 2 недели):
  docker builder prune -af   → очищает build cache
  docker image prune         → удаляет неиспользуемые образы

Если нужно освободить ~11GB:
  docker stop/rm/rmi auto-search-* shopify-bot-*  → не нужны GAD

Горячий деплой (если disk < 1GB):
  scp file.ts → docker cp → docker exec build → docker restart
```

---

## Авто-лончер — текущий статус (18.06.2026)

```
TREND_AUTO_LAUNCH_ENABLED=true   ← включён в .env VPS
ANTHROPIC_API_KEY=<реальный ключ> ← инжектирован в dist/index.js
                                    (НЕ персистентно — теряется при recreate!)
TREND_MIN_ENGAGEMENT=100         ← достаточно для GDELT трендов
TREND_LAUNCH_COOLDOWN_H=4        ← 4 часа между запусками

Тест запуска ПРОШЁЛ:
  $PENGOO ("Penguin Gang Inu") запущен 18.06.2026
  CA: C2eBqwJKU5Yrb1vMwcN6x9XSd2u1v4fyzGCcuocujgKM
  Dev buy: 0.03 SOL через PumpPortal ✅
```

---

## Деплой чеклист

```bash
# Локально:
git push gad main

# На VPS:
ssh -i ~/.ssh/gad_deploy root@65.21.159.255
cd /opt/gad-ai-terminal/GAD-AI-Terminal
git pull origin main

# Пересобрать сервис (только если > 500MB свободно):
docker compose build <service>
docker compose up -d --no-deps <service>

# Горячий деплой (при тесном диске):
scp -i ~/.ssh/gad_deploy file.ts root@65.21.159.255:/tmp/
docker cp /tmp/file.ts gad-ai-<service>:/usr/src/app/services/<service>/src/
docker exec gad-ai-<service> sh -c 'cd /usr/src/app && npm --workspace services/<service> run build'
docker restart gad-ai-<service>

# Проверка:
docker compose ps
docker logs gad-ai-<service> --tail=20
```

---

## Известные проблемы (22.06.2026)

| # | Проблема | Приоритет | Статус |
|---|---|---|---|
| 1 | **Disk VPS** — было 100%, сейчас 16% (32GB free) | ✅ РЕШЕНО | `tune2fs` + естественная очистка |
| 2 | **ANTHROPIC_API_KEY** не в .env | ✅ РЕШЕНО | Добавлен 19.06 |
| 3 | **OPENAI_API_KEY** не в .env | ✅ РЕШЕНО | Добавлен 20.06 |
| 4 | **GROQ_API_KEY** не в .env | ✅ РЕШЕНО | Добавлен 22.06 + scorer.ts hot-patched |
| 5 | Polymarket scorer: Claude кредиты кончились | ✅ РЕШЕНО | Groq→DeepSeek→OpenAI цепочка работает |
| 6 | pumpdotfun-sdk 1.4.2 сломан | 🔴 КРИТИЧНО | pump.fun добавил 5 новых аккаунтов (февр 2026). Fix: `github.com/D3AD-E/pumpdotfun-sdk-repumped` |
| 7 | TON holder check использует tonapi v2 формат | 🟡 СРЕДНЕ | `percentage` field может называться по-другому в ответе TonAPI — нужно проверить на реальных данных при включении |
| 8 | BSC scanner: пустой кошелёк | 🟠 ВАЖНО | Нужно пополнить BNB |
| 9 | W1 wallet: ~0.019 SOL (мало) | 🟠 ВАЖНО | Пополнить до ≥0.15 SOL — иначе autobuy не сработает |
| 10 | Polymarket LIVE: нужно WR≥65% | ⏳ ЖДЁМ | DRY-RUN, 1/30 сделок. НЕ менять POLYMARKET_DRY_RUN=true |
| 11 | BSC SwapSim не интегрирован | 🟡 СРЕДНЕ | tsconfig обновлён, но bsc-scanner/index.ts не вызывает simulateEvmSwap() |
| 12 | Twitter API 402: нет платного плана | 🟡 СРЕДНЕ | Nitter RSS fallback работает (без engagement метрик) |
| 13 | GMGN API: Cloudflare блокирует VPS | 🟡 СРЕДНЕ | Residential proxy $15/мес если нужно |
| 14 | Raydium не покупает (22.06) | ℹ️ РЫНОК | F&G=20, mom=0 — боковой рынок. Бот в порядке, ждёт сигнала |
